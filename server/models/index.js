const User = require('./User');
const Post = require('./Post');
const Follow = require('./Follow');

// Aqui você pode montar associações caso use ORM.
// Exemplo com Sequelize, se for o caso.

module.exports = { User, Post, Follow };
